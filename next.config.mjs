/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos','imgs.search.brave.com','images.unsplash.com','firebasestorage.googleapis.com'],
      }
    
};

export default nextConfig;
